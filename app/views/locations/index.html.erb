<% if flash[:notice] %>
<%= flash[:notice] %>
<% end %>

<h1>Locations</h1>

<ul>
	<% @locations.each do |location| %>
	<li><%= link_to ((location.name ? "#{location.name} - " : "") + location.city), location_visits_path(location) %></li>
	<% end %>
</ul>

<%= link_to "Create New Location", new_location_path %>